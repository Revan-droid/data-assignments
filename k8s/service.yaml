apiVersion: v1
kind: Service
metadata:
  name: cdc-lakehouse-inspector
  namespace: cdc-lakehouse
spec:
  selector:
    app: cdc-lakehouse-inspector
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP

